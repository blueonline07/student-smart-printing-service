import { StyleSheet } from 'react-native';

const styless = StyleSheet.create({
    fileInfo: {
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: 10,
    },});
export default styless;